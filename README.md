# CircuitBenchFaultSim
A circuit bench fault simulator written in Python. This can simulate node/wires/inputs that are faultily stuck at on/off values and will show how it affects the rest of the circuit.
